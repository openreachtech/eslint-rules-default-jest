/*
 * eslint-plugin-jest carries no deprecated rule. The ruleset is kept so that
 * the sibling packages expose the same surface, and so that the test for the
 * rule coverage can state that the plugin has none.
 */
export default {
  rules: {},
}
